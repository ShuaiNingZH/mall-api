<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.meet.mapper.permission.menu.SysMenuMapper">

    <!-- 根据 userId 查询权限标识(perm) -->
    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.perm
        FROM sys_user_role ur
                 INNER JOIN sys_role r ON ur.role_id = r.id
                 INNER JOIN sys_role_menu rm ON ur.role_id = rm.role_id
                 INNER JOIN sys_menu m ON rm.menu_id = m.id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
          AND r.is_deleted = 0
          AND m.status = 1
          AND m.is_deleted = 0
          AND m.perm IS NOT NULL
          AND m.perm &lt;&gt; ''
    </select>

    <!-- 根据 userId 查询角色编码 -->
    <select id="selectRoleCodesByUserId" resultType="java.lang.String">
        SELECT DISTINCT r.role_code
        FROM sys_user_role ur
                 INNER JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
          AND r.is_deleted = 0
    </select>

    <!-- 根据 userId 查询所有可访问菜单(含目录、菜单、按钮) -->
    <select id="selectMenusByUserId" resultType="com.atguigu.meet.model.entity.permission.menu.SysMenu">
        SELECT DISTINCT m.*
        FROM sys_menu m
                 INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
                 INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
                 INNER JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
          AND r.is_deleted = 0
          AND m.status = 1
          AND m.is_deleted = 0
        ORDER BY m.sort ASC
    </select>

</mapper>